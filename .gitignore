node_modules/
logs/
*.log

# Secrets — never commit any of these.
.env
.env.*
!.env.example
*.keypair.json
*-keypair.json
id.json
catnip-devnet.json
wallet*.json
**/secrets/

# Build / OS noise
dist/
coverage/
.DS_Store
Thumbs.db

# Video build artefacts. demo.mp4 (the one canonical cut) IS committed, and so
# are the narration clips in demo/audio/, so a clone can re-render the exact
# video without a TTS service. demo_raw.mp4 is the pre-compression intermediate.
demo/frames/
demo/check/
demo_raw.mp4
